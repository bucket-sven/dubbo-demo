//apply plugin: "war"
archivesBaseName = 'dubbo-consumer'

dependencies { 
  // 依赖 dubbo-interface 模块
  compile project(":dubbo-interface")
  implementation('org.springframework.boot:spring-boot-starter-web')
}
